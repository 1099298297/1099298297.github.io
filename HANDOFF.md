# 雾屿 · Misty Isle — 交接文档

给接手这个项目的 AI。请先读完「设计哲学」和「已知陷阱」两节再动手改代码，能省掉至少三轮返工。

> **2026-09-20 更新（第二任接手者）**
> 站点已从「单文件 index.html」拆成 `index.html + assets/style.css + assets/app.js`，
> 内容是 `content/` 下的 Markdown，由零依赖的 `build.mjs` 生成 `assets/content.js` 与 `dist/`。
> 第 0/2 节的文件与行号说明已过时，但**第 1 节的设计哲学和第 3 节的陷阱全部仍然有效**。
> 第 4 节列的功能已完成大半，完成情况见该节表格。日常写作与部署看 [README.md](README.md)。

---

## 0. 交付物

| 文件 | 说明 |
|---|---|
| `index.html` | 全站。单文件，CSS / JS / SVG 滤镜 / 图形全部内联，零外部依赖，断网可用，双击即开 |

当前版本：1655 行，约 93 KB。**没有构建步骤、没有 npm 依赖、没有图片资源**（所有配图都是 CSS 渐变生成的）。

---

## 1. 设计哲学（最重要，先看这个）

这是一块**会随季节变色的玻璃**。整个站建立在一条铁律上：

> **玻璃好不好看，八成取决于它背后有没有光。**

所以永远是先造光、再贴玻璃。任何"加一块 blur"的操作，如果背后没有值得透过的东西，结果一定是脏的。

三条由此派生的规则：

1. **模糊值即层级** — 导航 22px / 卡片 14px / 阅读层 40px。层级靠模糊值区分，不用文字标注。
2. **玻璃边缘永远比中心亮** — 每块玻璃顶部一道 1px `inset` 白高光。
3. **叠噪点** — 全站一层 5% 的 SVG 噪点。去掉"数字味"，给物理材质感。

### 双维度气候系统

这是本站的核心创意，把"昼夜切换"拆成两个维度：

- **季节（春/夏/秋/冬）管「光是什么颜色」** — 4 组 CSS 变量，定义光斑色、底色、墨色、强调色
- **天气（晴/雨/雾/雪）管「光是什么质地、玻璃上在发生什么」** — 4 组 CSS 变量，定义 `blur`、`saturate`、玻璃白度 `--gw`、描边强度 `--wg`、暗角、噪点

**4 × 4 = 16 种光，不是 16 套配色**。同一套季节色，四种天气是四种天气感（例如雨天 `orb-blur` 92px + 降饱和 + 压暗 + 暗角 0.26；雾天 126px + 光斑放大；雪天玻璃白度 0.78）。

用户交互入口是右下角的**气候罗盘**：外环四季、内环四天气，选中项转到 12 点，节点反向旋转保持正立。

---

## 2. 代码结构

单文件，`<style>` → `<body>` → `<svg>` 滤镜定义 → `<script>`（IIFE，严格模式）。

### CSS 变量分层

```
:root            通用令牌（圆角、缓动、字体、模糊档位）
[data-season=x]  --orb-1..5 / --bg-a..c / --ink / --ink-soft / --ink-faint / --accent / --warm
[data-weather=x] --gw（玻璃白度）/ --wg（描边强度）/ --sat / --orb-blur / --vig / --grain / --bg-sat / --bg-bri
```

所有颜色、粒子、光标光晕都读变量。**改配色只需改一组变量，不要硬编码颜色。**

### JS 主要模块（按文件内顺序）

| 位置 | 职责 |
|---|---|
| `SEASON` / `WEATHER` / `COMBO` / `FOOT` / `HERO_EM` | 16 种组合的文案与数据（温度、在读的书、在听的歌、标签、页脚寄语） |
| `JIEQI` / `solarTerm()` | 二十四节气（近似日期表 + 距节气天数） |
| `apply(fx)` | **核心**。写 `data-season` / `data-weather`，刷新全部文案与 UI，触发粒子重建 |
| `rotateRings()` | 罗盘旋转，节点反向抵消 |
| `buildWeather()` | 按天气重建粒子数组 |
| `frame(now)` | 主渲染循环，双 canvas（`#fxBack` 背景层 / `#fxFront` 前景层） |
| `initFilm()` / `initWet()` | 雨天水膜层注入与滤镜白名单 |
| `POSTS` / `CAPS` | 文章与图集数据 |
| `openHTML()` / `closePost()` | 阅读层 |

### 各天气的实现要点

- **晴** — `beams()` 画斜射光束（丁达尔），**太阳位置跟随鼠标**；粒子随季节换：春花瓣 / 夏浮尘 / 秋落叶 / 冬冰晶（亮到阈值会炸出十字星芒）
- **雨** — 背景散景灯光被雨往下拖出尾巴；阵雨节奏（每 9~22 秒雨变斜、灯变亮）；玻璃上的 `.film` 水膜层
- **雾** — 11 团巨雾横向流动 + 远处散景呼吸；**玻璃真的会起雾，鼠标划过可擦开，约十几秒后重新糊上**（这点用户评价最高，不要丢）
- **雪** — 三层景深，近层是手绘六角雪花（三主轴 + 六分枝、自转），全局风是三个不同频率正弦叠加，所以雪会一阵一阵地斜

---

## 3. 已知陷阱（血的教训）

### ⚠️ 陷阱一：SVG 滤镜绝对不要碰文字

**这是本项目踩过的最大的坑，直接导致两次返工。**

曾经用 `feMorphology`（erode 半径 2px）区分"中心/边缘"来做色散，结果是卡片内所有小字**全部消失**——细笔画被腐蚀吃光了。同一次还踩了 `core OUT SourceAlpha` 恒等于空的逻辑错误。

**规则**：
- 滤镜只作用于**大号文字**（Hero `h1`、`.lede`）或**纯装饰层**
- 卡片内的文字**一律不加任何滤镜**
- 当前 `initWet()` 是白名单制，全站只有 2 个元素带 `data-wet`。**扩展时保持白名单制，不要改成 `.card > *` 这类通配**

验证方法：切到雨天，逐块检查卡片文字是否完好。

### ⚠️ 陷阱二：backdrop-filter 拿不到背后内容

`backdrop-filter` 与 SVG `filter` **无法叠加做"背景先模糊、再被弯折"**。SVG 滤镜只能作用于元素自身渲染结果，拿不到它背后的东西。

所以 Apple 那种 Liquid Glass 的"边缘把背后内容弯曲放大"**在纯 CSS/SVG 下做不到原生**，只能分层近似。**不要在这一点上反复尝试**。

### ⚠️ 陷阱三：性能红线

- `backdrop-filter` 叠三层以上开始掉帧
- SVG 滤镜开销随面积增长很快，必须配 `IntersectionObserver` 只给视口内元素启用
- 不要在每帧里 `createLinearGradient` / `createRadialGradient`（曾经每帧建上百个，直接卡死）。雨丝已改成纯色描边
- 已有的保命机制，扩展时请保留：
  - `prefers-reduced-motion` → 粒子全静止
  - `visibilitychange` → 后台暂停渲染
  - 连续掉帧（`dt > 28` 累计 26 次）→ 自动降级 `data-fx="low"`
  - 移动端 / 窄屏 → 默认 `data-fx="low"`
  - 粒子数量按 `area = (W*H)/1440000` 自适应

### ⚠️ 陷阱四：`--gw` / `--wg` 是全局开关

不要单独调某块玻璃的透明度。天气切换靠这两个变量统一驱动全站玻璃的白度与描边强度。硬编码会破坏"同一套季节色、四种天气感"的一致性。

---

## 4. 当前未完成的功能（按优先级）

> 完成情况（2026-09-20）：P0 全清；P1 全清；P2 的「真实天气 API」「季节按日期自动推进」也已实现。
> 剩下的只有：16 种光各自的专属配图、真正的框架化（Astro/Vite）。

### P0 — 明显的残缺

1. ~~社交链接全是 `href="#"`~~ → 接进 `content/site.json`；RSS 由构建生成真的 `feed.xml`，邮箱点击即复制，没填的链接会明确提示而不是静默跳 `#`。
2. ~~只有 3 篇文章却写着第 128 篇~~ → 首页数字与卡片 `NO.x` 全部按真实文章数算出。
3. ~~图集、碎片硬编码~~ → 改为 `content/fragments/`、`content/gallery/` 下的 Markdown，数量自适应。
4. ~~文章列表与 `POSTS` 是两套数据~~ → 已合并为单一数据源（`content/`），渲染函数负责出卡片。

### P1 — 体验补全

5. ~~阅读层没有上一篇/下一篇~~ → 阅读层底部有「更早的一篇 / 更新的一篇」，键盘 `←` `→` 也能翻。
6. ~~没有搜索、标签筛选、归档~~ → `#panel` 面板：搜索标题/正文/标签/分类、标签云、按年归档，`/` 或 `Ctrl+K` 唤起。
7. ~~碎片与「此刻」卡不联动~~ → 「此刻」卡与碎片里的标签都可点，点了就筛选碎片区。
8. 「此刻」卡的书/歌仍写在 `app.js` 的 `SEASON` 表里（属于气候文案的一部分，没有外置）。
9. ~~移动端没有替代入口~~ → 小屏汉堡菜单，含全部区块 + 搜索。
10. ~~focus 态没设计~~ → 统一 `:focus-visible`；文章卡、图集格、归档条目都能键盘操作。

### P2 — 进阶

11. ~~接真实天气 API~~ → `◎` 拉 Open-Meteo（武汉，无需 Key），映射成晴/雨/雾/雪并显示实况温度；失败静默回退。
12. ~~季节按真实日期自动推进~~ → 首次访问和点 `◎` 都按当天日期定季节（`seasonOfDate`）。
13. 16 种光各自的专属配图（现在仍是 CSS 渐变）。
14. 转 Astro / Vite（现在已经能用 Markdown 写文章，是自己写的零依赖构建）。

---

## 5. 给接手者的建议

**先做 P0 的第 4 条**（合并文章数据源）。它是后续所有内容功能的基础：现在 HTML 和 JS 两套数据并存，加一篇文章要改两处，迟早不同步。合并成 JS 单一数据源 + 渲染函数之后，搜索、标签、归档才有落点。

**做内容功能时，注意别破坏视觉。** 这个站的价值八成在视觉与氛围上。加列表、搜索框、分页这些"功能性 UI"时，请：

- 用 `.glass` / `.pill` / `.tag` 这些既有基元，不要另起一套样式
- 新 UI 也要吃 `--gw` / `--wg`，否则切天气时会"浮"在页面氛围之外
- 新的交互元素记得加进光标 hover 列表（`$$('a,button,.post,.cell,.f,.pill,.tag')`）和 `.film` 注入列表

**验证清单**（改完必测）：

1. 16 种季节 × 天气组合各切一遍，无报错、文案正确、粒子正常
2. 雨天：卡片文字完好（这是历史 bug，优先看）
3. 雾天：鼠标能擦开玻璃，且会慢慢糊回去
4. 缩放窗口 / 移动端窄屏：粒子重建、滤镜 region 更新、无溢出
5. 切到别的标签页再切回来：渲染恢复、时间不跳变
6. 阅读层：打开、Esc 关闭、滚动、小屏

---

## 6. 用户偏好

- **明确要求过"发挥想象力，不必严格遵循我的话"**。给方向而非清单，期待主动设计。
- 但对**效果质量要求很高**：多次因为"做得丑""很卡"要求返工。**视觉品质和性能 > 功能数量。**
- 喜欢有物理依据的效果（折射、色散、景深、表面张力），反感"贴图式"的假效果。
- 会说实话，发现不对会直接指出。不要为了显得完成度高而掩盖问题。
